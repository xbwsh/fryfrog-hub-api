services:
  fryfrog-hub-api:
    image: ghcr.io/xbwsh/fryfrog-hub-api:latest
    # 本地构建改用下面这行，注释掉上面的 image
    # build: .
    container_name: fryfrog-hub-api
    restart: unless-stopped
    network_mode: host
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    volumes:
      - ./db:/data
