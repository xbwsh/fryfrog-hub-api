<?xml version="1.0" encoding="UTF-8"?>
<!--
  FryfrogHub 日志配置
  - 仅写文件，不输出控制台（生产规范；本地排查直接 tail logs/fryfrog-hub.log）
  - 级别：LOGGING_LEVEL_ROOT / LOGGING_LEVEL_APP 环境变量覆盖（默认 INFO）
  - traceId：由 TraceIdFilter 写入 MDC；后台调度线程自设 sched-* 前缀
  - 文件：logs/fryfrog-hub.log，按天 + 50MB 切割，保留 14 天 / 1GB
-->
<configuration scan="true" scanPeriod="30 seconds">

    <!-- 日志目录：默认相对启动工作目录的 logs/（IDEA 运行配置请把 Working Directory 设为项目根，
         Docker 部署用环境变量 LOG_HOME=/data/logs 固定到挂载卷） -->
    <property name="LOG_HOME" value="${LOG_HOME:-logs}"/>
    <property name="ROOT_LEVEL" value="${LOGGING_LEVEL_ROOT:-INFO}"/>
    <property name="APP_LEVEL" value="${LOGGING_LEVEL_APP:-INFO}"/>

    <!-- 统一输出格式：时间 级别 [traceId] 线程 logger - 消息；无 traceId 显示 sys -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{traceId:-sys}] [%thread] %logger{40} - %msg%n"/>

    <!-- 业务日志文件（异步滚动） -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/fryfrog-hub.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/fryfrog-hub.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>4096</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
        <appender-ref ref="FILE"/>
    </appender>

    <!-- 错误专文件：仅 ERROR 级，巡检/告警只盯这一个 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <file>${LOG_HOME}/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>20MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="ASYNC_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>1024</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
        <appender-ref ref="ERROR_FILE"/>
    </appender>

    <!-- 框架噪音收敛 -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>
    <!-- 音频标签库的 chunk 级 INFO 噪音 -->
    <logger name="org.jaudiotagger" level="WARN"/>

    <!-- 业务代码：独立可控 -->
    <logger name="com.fryfrog.hub" level="${APP_LEVEL}"/>

    <root level="${ROOT_LEVEL}">
        <appender-ref ref="ASYNC_FILE"/>
        <appender-ref ref="ASYNC_ERROR"/>
    </root>
</configuration>
