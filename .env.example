# SQLite（默认 data/fryfrog.db，可改路径）
SQLITE_PATH=data/fryfrog.db

# Application
SERVER_PORT=20058

# TMDB
TMDB_API_KEY=
TMDB_LANGUAGE=zh-CN

# Auth
AUTH_ENABLED=true
# 首次启动创建 admin 的密码；留空则自动生成随机密码（见启动日志）
AUTH_PASSWORD=
AUTH_TOKEN_TTL=604800
AUTH_LOGIN_MAX_FAILURES=5
AUTH_LOGIN_LOCK_MINUTES=15

# 媒体 / 代理（可选）
VIDEO_ROOT_PATHS=
FFMPEG_PATH=
PROXY_HOST=
PROXY_PORT=
SUBSONIC_ENCRYPT_KEY=
